{
  "name": "roadmap-assistant",
  "version": "1.0.0",
  "description": "AI-powered product roadmap assistant that balances company goals with customer feedback",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start"
  },
  "keywords": ["ai", "roadmap", "product-management", "customer-feedback"],
  "author": "Roadmap Assistant Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 